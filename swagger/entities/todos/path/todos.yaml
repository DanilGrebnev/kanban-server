post:
  summary: Create todo
  tags:
    - todos
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/todos/create-todo-body.yaml'
  responses:
    200:
      description: Success response
      content:
        application/json:
          schema:
            $ref: '../../../responses-schemas/success-response-schema.yaml'
delete:
  summary: Delete todo
  tags:
    - todos
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/todos/delete-todo-body.yaml'
  responses:
    200:
      description: Success response
      content:
        application/json:
          schema:
            $ref: '../../../responses-schemas/success-response-schema.yaml'

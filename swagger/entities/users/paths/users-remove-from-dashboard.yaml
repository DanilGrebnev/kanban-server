delete:
  summary: Delete user from dashboard
  tags:
    - users
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/users-remove-from-dashboard/body-schema.yaml'
  responses:
    '200':
      description: Success response
      content:
        application/json:
          schema:
            $ref: '../../../responses-schemas/success-response-schema.yaml'
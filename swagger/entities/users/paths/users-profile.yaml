get:
  tags:
    - users
  summary: Get user profile
  responses:
    "200":
      description: Get profile account
      content:
        application/json:
          schema:
            $ref: '../schemas/profile-schema.yaml'
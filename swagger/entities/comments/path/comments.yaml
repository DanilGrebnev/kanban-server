post:
  tags:
    - comments
  summary: Create comment
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/comments/create-comments-body.yaml'
  responses:
    200:
      description: Success response
      content:
        application/json:
          schema:
            $ref: '../../../responses-schemas/success-response-schema.yaml'